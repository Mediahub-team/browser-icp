---
type: template
stage: 8
status: done
tags: [stage/8, template, landing, designer-brief]
---

# ТЗ для дизайнера — лендинг <оффер / ICP-NN>

> Техническое задание на дизайн **пред-продуктового лендинга**: продукт ещё не запущен, единственная
> цель страницы — **заявка в лист ожидания** (waitlist) + снятие intent-сигнала. Заполняется из
> оффера (`07-offers/`), ICP и его customer language (`02-icp/`, `06-analysis/`) и описания продукта
> (`00-product/product-description.md`).
>
> ⚠️ **Честность (K3).** Лендинг не обещает **связывающих цифр** (кешбэк %/сумма/срок) и ничего
> против **инвариантов** [[product-functionality]] §2. Иллюстрации показывают продукт честно — не
> рисуют того, чего нет. Над-обещание убивает доверие ещё до запуска.
>
> 🎯 **Заполнять под исследуемый угол.** Все примеры ниже — плейсхолдеры `<…>`: подставить посыл,
> язык и доказательства **текущего** угла/оффера, не подмешивать чужой.
>
> 🛠 **Формулировки — обязательно через скилл `russian-copywriting`.** H1, подзаголовок, CTA, буллеты,
> микрокопи формы — генерируем скиллом: живой русский «ты/вы», без канцелярита/штампов, по ФЗ-38, через гейт.
> 🚫 **Не пробрасывать формулировки из этапов 0–7.** Из оффера/ICP берём **факты,
> ограничения и customer language как сырьё** (голоса клиента — в кавычках), но **тексты страницы пишем
> заново**. Дословный перенос фраз оффера в лендинг запрещён: те же факты — новыми словами.
> ✅ **Исключение — message match внутри этапа 8: H1 = фраза-обещание креатива связки, дословно.**
> Человек кликнул на обещание — первый экран обязан показать то же обещание (ad-scent). Ниже
> первого экрана — пишем заново, как обычно.

---

## 1. Контекст и цель
- **Продукт (1 фраза):** `<elevator из product-description §0>`.
- **Стадия:** пред-запуск. **Единственная цель страницы:** заявка в лист ожидания (e-mail).
- **Сегмент / угол:** ICP-NN — `<ведущий рычаг/мотив>`; **на какую боль бьём:** `<боль из этапа 6>`.
- **Откуда трафик:** Яндекс Директ (Поиск/РСЯ) / VK / Telegram → метка кампании в **UTM**.
- **Главный KPI страницы:** **CPS** (стоимость заявки) + распределение intent-ответов (см. §11).

## 2. Аудитория и тон
- **Кто и контекст:** `<краткий портрет ICP + что для него «изменение, которое покупают»>`.
- **Tone of voice:** `<например: спокойный/честный · простой, «по делу» · энергичный>` — обосновать сегментом.
- **Customer language (вставить ДОСЛОВНО в тексты):** «`<фраза 1>`», «`<фраза 2>`», «`<фраза 3>`».
- **Слова-табу (НЕ употреблять):** `<жаргон/буззворды, отталкивающие сегмент>`.

## 3. Описание и нарратив страницы
> Один абзац: **какую историю** страница рассказывает сверху вниз и **с каким единственным выводом**
> уходит пользователь. Это смысловой каркас — секции (§4) его раскрывают.

- **Главный вывод (one takeaway):** «`<что человек должен понять и почувствовать за 10 секунд>`».
- **Логика убеждения:** боль → обещание (оффер) → почему верить (RTB/честность) → лёгкое действие (waitlist).

## 4. Структура секций (сверху вниз) — что в каждой
> Скелет ниже — базовый, **глубина и порядок зависят от уровня осознанности сегмента**: боль
> осознана остро (problem-aware) → можно короче и сразу к механике; боли нет (convenience) →
> страница длиннее, сначала проблематизация. Обосновать выбор в §3.
1. **Hero.** H1 = **фраза-обещание креатива связки, дословно** (см. шапку) + подзаголовок (механика/обещание) + **CTA** + честный демо-визуал (концепты — §5.1). В первых двух экранах — **конкретный сценарий покупки**, не только абстракция.
2. **Ценность — 3 буллета.** Три выгоды языком клиента (из оффера), каждая — одна мысль. Если оффер несёт сценарную математику — показать её здесь (цифры покупок клиента; цифры выплат — только при утверждённых условиях пилота).
3. **Demo / «как это работает».** Визуальное объяснение механики (схема/мокап/короткая анимация), 2–3 шага. **Не замазывать смену канала:** если от человека требуется покупать иначе, чем он привык (другой браузер вместо приложения), — сказать это прямо и показать, чем компенсируется.
4. **Возражения.** **Каждое возражение из customer language (§2) получает адресующую секцию или блок** — особенно «у меня уже есть X, чем вы лучше» (сравнение с текущим решением сегмента: что остаётся, что добавляется). Возражение №1 из оффера — обязательный блок.
5. **Доверие / честность.** На чём держится обещание; честная граница («что пока не делаем»); анти-возражение «обманут с кешбэком» (визуал — §5.1).
6. **Форма waitlist.** Лестница действия (§6): e-mail → платформа → intent-блок (§11).
7. **Соц.доказательство.** До запуска — **блок «от основателя»** (кто делает и зачем, build-in-public) вместо плейсхолдеров цитат; цитаты и счётчики — только реальные. Цитаты синтетических респондентов на страницу не выносить.
8. **Футер.** Короткий, без отвлекающих ссылок; правовые ссылки (оферта/политика) если нужны.

## 5. Копирайтинг (точные тексты и микрокопи)
> Дизайнер не выдумывает тексты — берёт отсюда. Заполнить под оффер; держать длину под формат.

- **H1 (hero, ≤ ~60 симв.):** «`<фраза-обещание креатива связки — дословно>`».
- **Подзаголовок (≤ ~120 симв.):** «`<механика/обещание простыми словами>`».
- **CTA-копи (кнопка):** «`<Записаться в лист ожидания>`» *(повтор после demo)*.
- **3 буллета ценности:** «`<…>`» · «`<…>`» · «`<…>`».
- **Заголовок demo-блока:** «`<Как это работает>`»; подписи к шагам: `<1…>`, `<2…>`, `<3…>`.
- **Блок доверия — заголовок + 1–2 фразы:** «`<…>`».
- **Лейбл поля формы / плейсхолдер:** «`<Ваш e-mail>`».
- **Микрокопи под формой:** «`<не спамим; напишем, когда откроем доступ>`».
- **Состояние «спасибо» (после заявки):** «`<Вы в списке. Спасибо!>`».
- **Текст ошибки формы:** «`<Проверьте e-mail>`».
- **Intent-вопрос и варианты:** см. §11.

## 5.1 Картинки — варианты по местам (скилл `image`)
> Как и тексты, картинки даём **вариантами**: 2–3 концепта на место, дизайнер выбирает/тестирует.
> Сцену подстраиваем под угол оффера; конкретных сумм и процентов на картинке не показываем.

**Hero (главный визуал обещания):**
- A. Спокойный экран браузера на привычном магазине, скромный бейдж «возврат начислен» без суммы. Чисто, без мелкого нечитаемого текста.
- B. Телефон в руке на странице оплаты обычного магазина, мягко подсвечен бейдж кешбэка. Нейтральный фон, без стоковых лиц.
- C. «До / после»: слева — ворох вкладок кешбэк-сервисов, справа — один спокойный экран браузера. Без агрессивных стрелок и капса.

**Demo / «как это работает» (механика, 2–3 шага):**
- A. Три плоских кадра-схемы «открыл магазин → оплатил → рубли на балансе», иконки в бренд-цвете, без перегруза.
- B. Один экран прототипа с подсвеченным текущим шагом и тонкой полосой прогресса. Реальный скриншот, не AI-интерфейс.

**Доверие / K3 (визуал надёжности):**
- A. Экран статуса начисления (реальная механика): нейтральные подписи шагов, без сумм; пометка «образец».
- B. Сдержанный блок со значком оферты/документа, палитра «надёжно». Без золотых монет, печатей «100 %» и галочек-гарантий.

> **Избегать везде:** стоковые улыбающиеся люди, золотые монеты и «дождь из денег», фиолетовые
> градиенты-клише, мелкий нечитаемый текст, выдуманные суммы/проценты и чужие скриншоты выплат,
> AI-генерация интерфейса (рисует несуществующие экраны — берём скриншот прототипа).

## 6. Обязательные элементы конверсии
- **CTA** минимум дважды (hero + после demo), визуально приоритетна.
- **Лестница действия** (вместо одинокого e-mail): шаг 1 — e-mail; шаг 2 — **квалификация
  платформы** «iPhone / Android» (продукт iOS — Android-заявки иначе засоряют сигнал, а сам
  вопрос — intent-сигнал); шаг 3 — intent-блок (§11); опционально — подписка на TG-бота/канал
  (для трафика из TG конвертит лучше e-mail и даёт канал реактивации). **Мерим глубину
  прохождения лестницы**, не только факт заявки: при околонулевой конверсии заявок сравнение
  связок тонет в шуме, глубина даёт градиент.
- Без давления/таймеров/«осталось N мест».

## 7. Визуальная иерархия и стиль
- **Приоритет внимания:** H1 → CTA → demo → доверие. Один доминирующий акцентный цвет — на CTA.
- **Палитра/настроение:** `<бренд-токены / mood>`; типографика: `<заголовочная/текстовая, размеры, межстрочный>`.
- **Imagery-правила (скилл `image`):** честные мокапы продукта, без фото-стоков-клише и фейк-скринов. Картинки — **WebP** + фолбэк, hero/OG 1200×630 < 200 КБ, `width`/`height` заданы (против CLS), ниже экрана `loading="lazy"`, alt у всех.

## 8. Адаптив и состояния компонентов
- **Mobile-first**, брейкпоинты: mobile `<360–430>` / tablet `<768>` / desktop `<1200+>`.
- **Состояния формы:** default · focus · loading (сабмит) · success (intent-блок) · error.
- **Состояния CTA:** default · hover/active · disabled.

## 9. Доступность (a11y)
- Контраст текста/фона ≥ WCAG AA; tap-target ≥ 44×44; видимый focus-state; alt у изображений; семантический HTML; навигация с клавиатуры.

## 10. Технические требования
- **Адаптив**, быстрая загрузка (оптимизированные ассеты, без тяжёлых библиотек).
- **Форма → цель Яндекс.Метрики «waitlist_signup»** (замер CPS); прокидывать UTM из URL.
- Без сторонних трекеров кроме Метрики.

## 11. Intent-сигнал (тест спроса) и измерение
- После заявки показать: микро-вопрос «**Что для вас главное?**» с вариантами `<под оффер>`, `<ещё>`, `<контрольный>`; чекбокс «Готов(а) попробовать первым(ой) и оставить отзыв».
- **Критерий успеха:** CPS ≤ `<порог>`; распределение intent-ответов как сигнал, какой угол резонирует (вход для этапа 9).

## 12. Что НЕ делать
- Не обещать цифр выплат вне утверждённых владельцем условий пилота (гейт K3 в оффере) и ничего против инвариантов.
- Не использовать слова-табу. Не делать целью установку приложения (продукта ещё нет). Без давления/таймеров.

## 12.1 Гейт качества (чек перед коммитом ТЗ)
- [ ] **5-секундный тест:** по hero понятно *что это* (категория названа), *что я получу*, *почему верить*.
- [ ] **Message match:** H1 дословно = фраза-обещание креатива связки.
- [ ] **Конкретный сценарий покупки** есть в первых двух экранах.
- [ ] **Возражение №1** сегмента имеет адресующий блок; сравнение с текущим решением сегмента есть.
- [ ] **Барьер смены канала** назван прямо, не замазан «как обычно».
- [ ] Соцпруф честный (основатель/реальное), платформа квалифицируется, глубина лестницы мерится.

## 13. Референсы и deliverables
- **Референсы:** `<ссылки/примеры тона и стиля>`.
- **Deliverables:** макет (mobile + desktop), исходник, **спецификация состояний формы** (default/loading/success/error), экспорт ассетов, спека брейкпоинтов.

---

## Источники
> **Отдельная секция для читающего** (markdown-рендер). Формат ссылок: frontmatter `sources:` и
> инлайн-упоминание `**Источники:**` вверху — **вики** `[[…]]` (Obsidian + трассировка + check-vault);
> **здесь** — те же источники **markdown-ссылками относительно этого файла** (`08-ads/landings/` →
> `../../` до корневых папок, `../` до соседних папок этапа 8). **Пути проверять** перед сдачей (файл открывается).
- Оффер: [offer-&lt;slug&gt;](../../07-offers/offer-<slug>.md)
- ICP: [icp-NN-&lt;slug&gt;](../../02-icp/icp-NN-<slug>.md)
- Описание продукта: [product-description](../../00-product/product-description.md)
- План тестов: [test-plan](../test-plan.md) · Креатив: [creative-&lt;slug&gt;](../creatives/creative-<slug>.md)

**Что дальше:** заполненный лендинг идёт в сплит-тест этапа 8 (связки — в test-plan); по итогам
открутки сверяем с гипотезами на этапе 9 (`09-results/`).
</content>
