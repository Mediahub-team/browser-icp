---
type: keyword-research
stage: 8
status: draft
tags: [stage/8, keywords]
---

# Сбор ключевых слов — <сегмент / ICP-NN> (Яндекс Директ)

> Артефакт-источник для таргета: сырой сбор поисковых фраз через MCP `bukvarix` + анализ
> частотности + курированные группы. На него ссылается [targeting-icp-NN](../targeting/targeting-icp-NN.md).
> Здесь живёт «кухня» (seed, выдача, решения), чтобы таргет-док оставался чистым.
>
> 🔑 **`russian-copywriting` НЕ применяем.** Ключи — это *данные*: сохраняем **реальные поисковые
> фразы как есть** (форма и орфография, как реально ищут — напр. «кэшбэк», не «кешбэк»).
> Гейт живого русского — только для текста объявлений (креатив).
> ⚠️ Частотность Bukvarix — **сигнал разведки, не финальный объём.** Финальный объём — прогнозатор Директа.

## 0. Привязка
- **ICP / аудитория:** [icp-NN-&lt;slug&gt;](../../02-icp/icp-NN-<slug>.md) · **Ведущий мотив:** …
- **Инструмент:** MCP `bukvarix` (`bukvarix_search_keywords` / `bukvarix_search_keywords_batch`).

## 1. Seed-фразы (гипотезы запросов)
> Формируем сами из ICP §6 (customer language) и §7 (каналы). Это вход для Bukvarix, не финал.
- `seed 1` — почему (какая боль/мотив за запросом)
- `seed 2` — …
- **Exclusions для сбора:** … (мусорный интент, который не хотим тянуть в выдачу)

## 2. Выдача и анализ (Bukvarix)
> Таблица: фраза → частотность (широкая) → решение. Сохраняем реальную форму.

| Фраза | Широкая частотность | Решение (ядро / точное / минус / выбросить) |
|---|---|---|
| … | … | … |

- **Коррекция орфографии/формы:** какую форму рынок реально ищет (напр. кэшбэк ≫ кешбэк, ×N).
- **Head vs long-tail:** что несёт объём, что нишевое.
- **Чужой интент → кандидаты в минус-слова:** …

## 3. Курированные группы (идут в таргет)
> Эти группы переносятся в [targeting-icp-NN](../targeting/targeting-icp-NN.md) как есть.
- **Группа A — &lt;ядро интента&gt;:** `фраза` частотность · `фраза` частотность · …
- **Группа B — &lt;точное попадание под продукт&gt;:** `фраза` частотность · …
- **Минус-слова:** … (из off-intent выдачи)

## 4. Находки для стратегии
- **Гипотеза по охвату:** узкий vs широкий интент по частотностям (риск reach из ICP §7 — подтверждён/опровергнут?).
- **Сигнал для креатива:** реальные формулировки/лексика для отзеркаливания (передаётся в creative, пишется заново через гейт).

---

## Источники
> **Отдельная секция для читающего** (markdown-рендер). Формат: frontmatter `sources:` и инлайн
> `**Источники:**` вверху — **вики** `[[…]]` (Obsidian + трассировка); **здесь** — те же источники
> **markdown-ссылками относительно этого файла** (`08-ads/keyword-research/` → `../../` до корневых
> папок, `../` до соседних папок этапа 8). **Пути проверять** перед сдачей (файл открывается).
- ICP: [icp-NN-&lt;slug&gt;](../../02-icp/icp-NN-<slug>.md)
- Анализ (customer language): [comparison-&lt;date&gt;](../../06-analysis/comparison-<date>.md)
- Таргет (потребитель): [targeting-icp-NN](../targeting/targeting-icp-NN.md)

**Что дальше:** курированные группы идут в таргет; реальные формулировки — сырьё для отзеркаливания в креативах (через `russian-copywriting`).
